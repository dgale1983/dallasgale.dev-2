import React from 'react'

import Logo from '../logo'
import Nav from '../nav'

const Header = () => (
  <header className="elements__header">
    <div>
      <Logo />
    </div>
  </header>
)

export default Header
